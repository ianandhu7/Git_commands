git config --global user.name "anandhuviju" configure git with our username

git config --global user.email "anandhuviju7@gamil.com">> configure git with our emailid

configuration requires while pushing towards a public respository in github from a local respository

git status >> used to check the status of .git in the current directory

get init >> to initialize git in the current directory (helps to track the changes in our directory)

* if we add,delete a file it will track it as a change in the directory

U >> represents untracked (file is there not yet commited or not added to staging area)

A >> files that added to staging area

M >> files being modified after commited

git add intro.txt >> intro.txt added to the staging area

git add. >> all files added to staging area

A >> files that added to staging area

git commit -m "my first commit"

git push >> u

git clone <respository> url >> for cloning a repository from github